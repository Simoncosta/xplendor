// Re-exporta o Kanban partilhado (Components/Common/TicketsKanban). O visual e a
// lógica vivem lá — este ficheiro mantém-se só para não partir imports antigos.
export { default } from "Components/Common/TicketsKanban";
