requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0
python-dotenv==1.0.1
# PingWin (MyCloudPie) — leitura de .xls via xlrd 1.x (⚠️ NÃO atualizar: última
# versão com suporte .xls). pandas p/ o DataFrame; openpyxl p/ .xlsx futuros.
pandas==2.2.2
xlrd==1.2.0
openpyxl==3.1.5
# OCR de faturas (Fase A): rasterizar PDF-scan → imagem para a IA. Requer
# poppler-utils no sistema (ver docker/scraper/Dockerfile). A chamada à IA é em PHP.
pdf2image==1.17.0
Pillow==10.4.0
